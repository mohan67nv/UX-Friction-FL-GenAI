/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using default output so `next start` works reliably in Docker.
  // (Standalone output requires starting `.next/standalone/server.js` instead.)
};

module.exports = nextConfig;
